import React from 'react';
import { storiesOf } from '@storybook/react';
import { ClipsPage } from 'components';

storiesOf('ClipsPage', module)
    .add('default', () => (
        <ClipsPage location={{ }}/>
    ));
